# Shinaic accent blog: local delivery checks

Checked 9 September 2026. The post is available at `/blogs/shinaic-accent` and is listed in Blogs. This work prepares the site locally; it does not deploy it or change lexical ancestry records.

## Content and reproducibility

- Seven main claims, each with an interactive quantitative chart, ten distinct linked family examples, individual annotations, and exceptions or explicit limits.
- Six appendices preserve the held-out Kalkoti, Gilgit, Kundal Shahi and Sauji findings, five etymological proposals, and the full evidence and counting-method discussion.
- 214 distinct linked dictionary, language and reference records resolve during rendering.
- `python3 scripts/verify_shinaic_blog.py`: **279 checks passed**. Checks reconstruct chart totals from the frozen tables, validate the seventy selected examples and appendix coverage, and verify exact exported assets.
- All 29 source downloads remain byte-identical to the frozen research files. The complete archive retains SHA-256 `cff67a3590fcffebc2f5177a4bc34f3abd99d71cdc6f53eef4825cb0208595e6`.
- Edit `research/shinaic-accent/blog-post-2026-09-09.md` and the example annotations in `scripts/build_shinaic_blog_evidence.py`, then regenerate with that script. The generated post is `src/lib/blog/posts/shinaic-accent.md`.

## Code and production checks

- `node --test tests/blog.test.mjs`: **6 tests passed**.
- `npm run check`: **0 errors**, seven existing warnings in five unrelated files; none in the new chart component or blog route.
- `git diff --check`: passed.
- Isolated production build: **passed**, including static adapter output. The smoke build used `PRERENDER_LIMIT=50` and disabled link crawling in a temporary copy only; the repository configuration was unchanged. This is not an exhaustive prerender of the entire lexical database.
- Isolation was necessary because running development servers share the repository's generated SvelteKit directory. An earlier shared-directory preview had mismatched server/client bootstrap assets; this disappeared with the isolated build.
- Production HTTP checks: seven server-rendered figures with exact count tables, six appendix anchors, 214 linked records, all 29 downloads returning their exact byte sizes, and the annotated example JSON returning successfully.

## Browser checks

- Standard Vite production preview at `http://127.0.0.1:4177/blogs/shinaic-accent`, served from `/private/tmp/jambu-shinaic-blog-qa-20260909` in tmux window `shinaic-quant-0909:blog-preview`.
- Desktop layout checked at 1280 × 900; mobile layout at 390 × 844. Mobile selectors fit their containers and evidence tables scroll horizontally without overflowing the page.
- Count and Proportion controls work in all seven figures. Source/morphological comparison selectors work; the seven-language comparison explicitly displays Kalkoti's zero-candidate domain. Exact count disclosures remain available for every scope.
- Static HTML includes charts and exact tables before hydration, so the evidence is readable without JavaScript.
- A linked crow etymon opened the correct dictionary entry, `/entries/2993`. The Blogs index listed the post and opened it successfully.
- No new browser console errors appeared in the final isolated production preview.

The original frozen report and research directory were not rewritten while preparing this post.
